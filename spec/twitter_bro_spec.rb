require 'spec_helper'

describe TwitterBro do
  it 'has a version number' do
    expect(TwitterBro::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
